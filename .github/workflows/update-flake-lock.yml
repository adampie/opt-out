---
name: "Update flake inputs"
"on":
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:
jobs:
  update-flake-lock:
    runs-on: "ubuntu-latest"
    permissions:
      contents: "write"
      pull-requests: "write"
    steps:
      - uses: "actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd" # v6
      - uses: "DeterminateSystems/determinate-nix-action@f9ebef276016dd519d188ee7b71879732418f8b9" # v3
      - uses: "DeterminateSystems/update-flake-lock@5909792a83875ddb5dd4b18734534a98a74a709c" # main
        with:
          pr-title: "Update flake inputs"
          pr-assignees: "adampie"
